{
  "name": "usb-cam-app",
  "version": "1.1.0",
  "description": "Android USB Camera App – 100% lokal, kein Cloud, kein Tracking. UVC + EasyCap + Capture Cards via USB-C OTG.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "setup:android": "node scripts/setup-android.mjs",
    "sync": "npm run build && npx cap sync android",
    "open:android": "npx cap open android",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease",
    "bundle:aab": "npm run sync && cd android && ./gradlew bundleRelease",
    "clean:android": "rimraf android",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/preferences": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3",
    "vite": "^6.3.5"
  }
}
